# Requirements for Nexus Journal — Literature Review Engine
# Optimized for CPU-only (AMD Ryzen 5 5600H, 14GB RAM, no GPU)

# Core framework
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
pydantic==2.11.3

# PDF parsing
PyMuPDF==1.25.5

# NLP / ML (CPU-only — no CUDA)
transformers==4.51.3
torch>=2.0.0
sentence-transformers==4.1.0
nltk==3.9.1

# Vector store
faiss-cpu==1.11.0
numpy>=1.24.0

# API clients
httpx==0.28.1

# Report export
fpdf2==2.9.1

# Environment
python-dotenv==1.1.0
